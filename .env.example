# Apple Silicon local build. The image contains /opt/orcaslicer/AppRun.
# Railway uses the corresponding AMD64 tag without the -arm64 suffix.
ORCA_RUNTIME_IMAGE=ghcr.io/itymarcel/custom-orca:sha-fd98397-arm64

SLICER_CONCURRENCY=1
SLICER_TIMEOUT_SECONDS=700
SLICER_MAX_FILE_BYTES=209715200
