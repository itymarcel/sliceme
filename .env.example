# The image must contain an executable at /opt/orcaslicer/AppRun.
# Keep the local tag below, or replace it with an image you can pull.
ORCA_RUNTIME_IMAGE=standalone-orca-runtime:local

SLICER_CONCURRENCY=1
SLICER_TIMEOUT_SECONDS=700
SLICER_MAX_FILE_BYTES=209715200
